# Grafana 부팅 시 자동으로 Prometheus 데이터소스를 등록한다 (수동 클릭 설정 불필요).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
